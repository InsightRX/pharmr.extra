% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patch_pharmpy_nlmixr.R
\name{patch_pharmpy_nlmixr_results}
\alias{patch_pharmpy_nlmixr_results}
\title{Patch Pharmpy's nlmixr execution backend}
\usage{
patch_pharmpy_nlmixr_results()
}
\value{
\code{NULL}, invisibly. Called for its side effect on the Python session.
}
\description{
Applies Python monkey-patches to \code{pharmpy.tools.external.nlmixr.run}, the
module every nlmixr candidate fit dispatched by a Pharmpy tool
(\code{bootstrap}, \code{modelsearch}, \code{covsearch}, \code{iivsearch}, \code{amd}) goes through.
Three upstream bugs make it fail for essentially every real dataset, so
those tools are unusable against nlmixr-format models without this patch
(InsightRX/pharmr.extra#121):
}
\details{
\enumerate{
\item \verb{rdata["thetas"].loc[get_thetas(model).names]} — \code{Parameters$names} is a
\emph{tuple} in Pharmpy >= 2.0, and \code{pandas} reads a tuple passed to \code{.loc} as
a multi-axis indexer, so this raises
\verb{IndexingError: Too many indexers}. (In Pharmpy 1.8 \code{names} is a list and
the line happens to work.)
\item \verb{predictions.set_index(model.dataset[model.dataset["DV"] != 0].index)} —
nlmixr2 returns one prediction row per \emph{observation record}, but this
indexes the dataset by "DV is non-zero". Any dataset with an observation
whose \code{DV} is exactly 0 (BLQ imputed to 0, a baseline 0 sample, ...)
therefore has fewer index labels than prediction rows and pandas raises
\verb{ValueError: Length mismatch}.
\item \code{execute_model()} writes the candidate's dataset with
\code{write_dataset()} / \code{write_csv()}, which names the file after the
\emph{datainfo} path, but the R script it generates reads
\verb{<model name>.csv}. Whenever the model carries a datainfo path — which
every \code{modelsearch} candidate does, since they are derived from the input
model — the two names differ and the R run dies with
\verb{cannot open file '.../<model name>.csv'}, leaving no results for the
tool to rank.
}

The patch fixes (1) by wrapping the \code{get_thetas} that \code{run.py} imported so
\code{names} is always a list, (2) by handing the parser a model whose \code{DV}
column is 1 on observation records and 0 elsewhere — so the upstream
\code{DV != 0} mask selects exactly the observation rows, with the dataset's own
index labels preserved (observation records are identified from \code{EVID} /
\code{MDV}; masks are tried in turn and the first one the parser accepts wins,
and if none work the unpatched call is made so the original Pharmpy error
surfaces) — and (3) by wrapping the dataset writer so that, when it is
handed a directory, it writes \verb{<model name>.csv}: the name the generated R
script actually reads.

Idempotent, and a no-op on a Pharmpy release that has fixed these bugs.
Called automatically by \code{\link[=call_pharmpy_tool]{call_pharmpy_tool()}} for nlmixr-format models.
}
