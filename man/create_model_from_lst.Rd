% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_model_from_lst.R
\name{create_model_from_lst}
\alias{create_model_from_lst}
\title{Create a Pharmpy model object from a NONMEM report file (\code{.lst}/\code{.res})}
\usage{
create_model_from_lst(lst_file, data = NULL, save_as = NULL, verbose = TRUE)
}
\arguments{
\item{lst_file}{path to a NONMEM report file (\code{.lst}, \code{.res}, ...).}

\item{data}{optional dataset (filename or \code{data.frame}) passed to
\code{\link[=create_model_from_file]{create_model_from_file()}}.}

\item{save_as}{optional path to write the resulting NONMEM model code to.
Default \code{NULL} (return the model object only).}

\item{verbose}{verbose output. Default \code{TRUE}.}
}
\value{
a Pharmpy model object.
}
\description{
For the case where only a NONMEM report file is available and the \code{.ext}
file (from which pharmpy normally reads final estimates) is missing. Reads
the embedded control stream and applies the final parameter estimates parsed
from the report's \verb{FINAL PARAMETER ESTIMATE} section.
}
\details{
Internally the parsed estimates are written to a synthetic \code{.ext} file and
fed through the existing \code{\link[=create_model_from_file]{create_model_from_file()}} path, so pharmpy handles
the NONMEM-name to parameter-name mapping.

\strong{Precision:} estimates recovered from a \code{.lst} are rounded to ~3
significant figures (see \code{\link[=parse_lst]{parse_lst()}}). Use an \code{.ext} file with
\code{\link[=create_model_from_file]{create_model_from_file()}} when full precision is required.
}
\seealso{
\code{\link[=parse_lst]{parse_lst()}}, \code{\link[=create_model_from_file]{create_model_from_file()}}
}
