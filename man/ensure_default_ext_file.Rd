% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensure_default_ext_file.R
\name{ensure_default_ext_file}
\alias{ensure_default_ext_file}
\title{Ensure the default \code{.ext} file exists in a NONMEM run folder}
\usage{
ensure_default_ext_file(fit_folder, model_file, verbose = TRUE)
}
\arguments{
\item{fit_folder}{path to the run folder.}

\item{model_file}{model filename (e.g. \code{"run.mod"}); the stem determines
the expected \code{.ext} name.}

\item{verbose}{emit a \code{cli} message describing the action. Default \code{TRUE}.}
}
\value{
Invisibly \code{TRUE} if a file was copied, \code{FALSE} otherwise.
}
\description{
NONMEM's \verb{$ESTIMATION FILE=...} option redirects the iteration output to
a non-default filename (e.g. PsN-style runs write \code{psn.ext}). pharmpy's
result reader derives the \code{.ext} path from the model stem (\code{run.mod} →
\code{run.ext}) and will fail silently when that file is missing or empty.
}
\details{
This helper is called after the fit completes. If \verb{<stem>.ext} is missing
or zero-bytes, and exactly one other non-empty \code{.ext} file is present in
the fit folder, that file is copied to \verb{<stem>.ext}. When zero or more
than one candidate is found, nothing is copied.
}
