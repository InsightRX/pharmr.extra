% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_modelfit_results.R
\name{read_modelfit_results}
\alias{read_modelfit_results}
\title{Read NONMEM modelfit results, with bug fixes for SIR covariance models}
\usage{
read_modelfit_results(path, ...)
}
\arguments{
\item{path}{Path to NONMEM model file (e.g. \code{"run1/run.mod"}).}

\item{...}{Additional arguments passed to \code{pharmr::read_modelfit_results()}.}
}
\value{
A Pharmpy \code{ModelfitResults} object (same as
\code{pharmr::read_modelfit_results()}).
}
\description{
Wraps \code{pharmr::read_modelfit_results()} with patches for Pharmpy bugs that
arise when a SIR covariance step is used (\verb{$COVARIANCE SIRSAMPLE=...}).
NONMEM writes a second ext-file table (method "Importance Sampling of
Variance-Covariance (SIR)") that Pharmpy mishandles in three ways:
}
\details{
\strong{Bug 1} (\verb{_parse_parameter_estimates} — NaN final estimates): The SIR
table contains ITERATION 0 (FOCE starting point) and iterations 1…N (SIR
samples). Because the SIR modal OFV at \code{-1000000000} differs from the last
sample OFV, \verb{_get_iter_df} appends a NaN sentinel row. This NaN row is the
last row of the combined \code{pe} DataFrame, so \verb{pe.iloc[-1].isnull().all()} is
True and \code{final_pe} is set to all-NaN instead of the FOCE estimates.

\strong{Bug 2} (\verb{_parse_table_numbers} — wrong covstatus): Including the SIR
table in \code{table_numbers} makes \code{covstatus_table_number} point to the SIR
table (number 2). The SIR block in the lst-file has "Elapsed estimation
time" but no "Elapsed covariance time", so \code{covariance_step_ok} is \code{None}
and the \code{.cov}/\code{.cor} files are never read, even though the standard
covariance step ran successfully under table 1.

\strong{Bug 3} (\verb{_parse_standard_errors} — cov_abort with missing -1000000005):
If the SIR ext table has row \code{-1000000001} (SEs) but not \code{-1000000005}
(omega/sigma SEs in stdcorr form), the original code overrides the valid
SEs with NaN and sets \code{cov_abort = True}, blocking the \code{.cov} file read.

The fixes skip the SIR table in \verb{_parse_table_numbers}, \verb{_parse_ofv}, and
\verb{_parse_parameter_estimates} (so only the FOCE table is used for estimates
and covariance-step lookup), while \verb{_parse_standard_errors} still uses the
last (SIR) table to extract SIR-based standard errors.
}
