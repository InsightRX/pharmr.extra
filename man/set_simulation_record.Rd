% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_simulation_clean.R
\name{set_simulation_record}
\alias{set_simulation_record}
\title{Set the \verb{$SIMULATION} record of a NONMEM control stream}
\usage{
set_simulation_record(code, seed, n, true_prior = FALSE)
}
\arguments{
\item{code}{NONMEM model code (single string or character vector of lines).}

\item{seed}{random seed number}

\item{n}{number of simulation subproblems to run}

\item{true_prior}{emit \code{TRUE=PRIOR}, i.e. have NONMEM draw a new parameter
vector from the model's \verb{$PRIOR} record for every subproblem (see
\code{\link[=add_nwpri_prior]{add_nwpri_prior()}}). Default \code{FALSE}.}
}
\value{
NONMEM model code as a single string.
}
\description{
Works on the model code rather than on a Pharmpy model object, because
Pharmpy cannot parse \code{TRUE=PRIOR} (see \code{\link[=set_simulation_clean]{set_simulation_clean()}}) and because
the \code{uncertainty_engine = "nwpri"} path of \code{\link[=run_sim]{run_sim()}} only varies the seed
and the subproblem count between chunks — a string edit on an otherwise
finished control stream.
}
\details{
An existing \verb{$SIMULATION} record is replaced in place, so its position
relative to the \verb{$TABLE} records is preserved. When the model has none, the
record is appended.
}
