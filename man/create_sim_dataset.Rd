% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_sim_dataset.R
\name{create_sim_dataset}
\alias{create_sim_dataset}
\title{Create a NONMEM dataset for simulation}
\usage{
create_sim_dataset(
  model,
  data = NULL,
  regimen = NULL,
  t_obs = NULL,
  covariates = NULL,
  n_subjects = NULL,
  input_from_data = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{model}{a Pharmpy model object, or a path to a NONMEM model file
(\code{.mod}). If a file path is supplied, the model is loaded with
\code{pharmr::read_model()} so that the \verb{$DATA} path can be resolved.}

\item{data}{optional data.frame (or path to a CSV file) to use as the base
dataset instead of the dataset attached to \code{model}. Useful when you want to
apply \code{t_obs} or \code{regimen} changes to an already-prepared dataset.
It is assumed that the column names in the dataset match the \emph{order} of the
columns in $INPUT in the model. If this is not the case, the creation of
the dataset may fail, or the simulations from the dataset may fail.}

\item{regimen}{if specified, will replace the regimens for each subject with
a custom regimen. Can be specified in two ways. The simplest way is to just
specify a list with elements \code{dose}, \code{interval}, \code{n}, and
\code{route} (and \code{t_inf} / \code{rate} for infusions).
E.g. \code{regimen = list(dose = 500, interval = 12, n = 5, route = "oral")}.
Alternatively, regimens can be specified as a data.frame. The data.frame
specifies all dosing times (\code{dose}, \code{time} columns) and \code{route} and
\code{t_inf} / \code{rate}. The data.frame may also optionally contain a \code{regimen}
column that specifies a name for the regimen. This can be used to simulate
multiple regimens.}

\item{t_obs}{a vector of observation times. If specified, will override
the observations in each subject in the input dataset.}

\item{covariates}{if specified, will replace subjects with subjects specified
in a data.frame. In the data.frame, the column names should correspond
exactly to any covariates included in the model. An \code{ID} column is optional;
if absent, IDs are generated as \code{1:nrow(covariates)}. For time-varying
covariates, a \code{TIME} column is also required (otherwise it will be assumed
covariates are not changing over time).}

\item{n_subjects}{number of subjects to simulate, when using sampled data
(i.e. requires \code{covariates} argument)}

\item{verbose}{logical; print progress messages.}
}
\value{
data.frame with a NONMEM-format simulation dataset. A \code{.regimen}
column is included and is used internally by \code{\link[=run_sim]{run_sim()}} to loop over
multiple dosing regimens.
}
\description{
Prepares a dataset for use with \code{\link[=run_sim]{run_sim()}}, handling covariate sampling,
regimen replacement, and observation record creation. The returned
data.frame can be passed directly to \code{\link[=run_sim]{run_sim()}} as the \code{data} argument.
}
