% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_bootstrap.R
\name{plot_bootstrap}
\alias{plot_bootstrap}
\title{Plot bootstrap parameter distributions}
\usage{
plot_bootstrap(
  bootstrap_results,
  original_estimates = NULL,
  ci = 0.95,
  parameters = NULL
)
}
\arguments{
\item{bootstrap_results}{Bootstrap results object returned by
\code{call_pharmpy_tool(tool = "bootstrap")}. Must expose a
\code{parameter_estimates} attribute (a pandas DataFrame that reticulate
auto-converts to an R data.frame).}

\item{original_estimates}{Named numeric vector of original parameter
estimates (e.g. from \code{run_nlme()} results) to overlay as a solid vertical
line on each panel. Names must match column names of
\code{bootstrap_results$parameter_estimates}. Parameters not found are silently
ignored. Pass \code{NULL} (default) to suppress.}

\item{ci}{Numeric confidence level for the displayed credible interval
(e.g. \code{0.95} draws the 2.5th and 97.5th percentile as dashed lines). Pass
\code{NULL} to suppress CI lines.}

\item{parameters}{Character vector of parameter names to include. \code{NULL}
(default) includes all parameters.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object. Use \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} to save it.
}
\description{
Creates a faceted density + histogram plot showing the distribution of
parameter estimates across bootstrap samples. Optionally overlays the
original (fitted) parameter estimate and confidence interval lines.
}
\examples{
\dontrun{
# After running bootstrap:
bs <- call_pharmpy_tool(
  id = "run1",
  model = model,
  tool = "bootstrap",
  options = list(samples = 200)
)

# Basic plot
plot_bootstrap(bs)

# With original estimates overlaid
orig <- c(POP_CL = 4.5, POP_V = 32.1, IIV_CL = 0.09)
p <- plot_bootstrap(bs, original_estimates = orig, ci = 0.95)
ggplot2::ggsave("bootstrap_params.png", p, width = 10, height = 8)
}

}
