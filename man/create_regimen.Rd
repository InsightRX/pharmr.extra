% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_regimen.R
\name{create_regimen}
\alias{create_regimen}
\title{Create a single regimen}
\usage{
create_regimen(
  dose,
  interval = 24,
  n,
  t_inf = NULL,
  per = NULL,
  route = c("oral", "iv", "sc", "im")
)
}
\arguments{
\item{dose}{TODO}

\item{interval}{TODO}

\item{n}{TODO}

\item{t_inf}{TODO}

\item{per}{character or \code{NULL}. Name of a column in the simulation dataset
whose value is used to scale each subject's dose (e.g. \code{per = "WT"} for
mg/kg dosing, \code{per = "BSA"} for mg/m² dosing). The final AMT for each
subject is \code{dose * covariate_value}. When \code{NULL} (the default), the dose
is applied as an absolute amount.}

\item{route}{TODO}
}
\value{
TODO
}
\description{
The resulting data.frame can be passed to \code{\link[=create_sim_dataset]{create_sim_dataset()}} as the
\code{regimen} argument.
}
\examples{

\dontrun{
reg1 <- create_regimen(
  dose = 500,
  interval = 12,
  n = 10,
  route = "oral"
)
create_sim_dataset(..., regimen = reg1)

# Weight-based dosing (5 mg/kg):
reg2 <- create_regimen(dose = 5, per = "WT", interval = 24, n = 5, route = "sc")
create_sim_dataset(..., regimen = reg2)
}

}
