% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_nonmem_data.R
\name{update_nonmem_data}
\alias{update_nonmem_data}
\title{Update the dataset path in NONMEM model code}
\usage{
update_nonmem_data(code, path)
}
\arguments{
\item{code}{NONMEM model code. Either a single character string (possibly
with embedded newlines) or a character vector of lines.}

\item{path}{path to the new dataset.}
}
\value{
The updated model code, in the same shape as \code{code}: a single
string if \code{code} was a string, a character vector otherwise.
}
\description{
Replaces the dataset path in the first \verb{$DATA} record with a new path,
while preserving every other element: any options on the \verb{$DATA} line
(\verb{IGNORE=@}, \code{ACCEPT=(...)}, \code{REWIND}, \code{RECORDS=N}, ...), any inline
comment on that line, and all other records in the model code.
}
\details{
The match is case-insensitive and also accepts the abbreviated \verb{$DAT}
record name. Dataset paths in the original record may be unquoted or
wrapped in single or double quotes. If the new \code{path} contains
whitespace, it is wrapped in single quotes so NONMEM parses it as one
token.
}
\examples{
code <- "$PROB Test\n$DATA old.csv IGNORE=@ ACCEPT=(DV.GT.0)\n$INPUT ID TIME DV"
cat(update_nonmem_data(code, "new.csv"))

}
