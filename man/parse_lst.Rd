% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_lst.R
\name{parse_lst}
\alias{parse_lst}
\title{Parse a NONMEM report file (\code{.lst}/\code{.res})}
\usage{
parse_lst(lst_file = NULL, code = NULL)
}
\arguments{
\item{lst_file}{path to a NONMEM report file (\code{.lst}, \code{.res}, ...).}

\item{code}{character string with the report file contents (alternative to
\code{lst_file}).}
}
\value{
a list with elements \code{control_stream} (character), \code{theta} (numeric
vector), \code{omega} (numeric matrix or \code{NULL}), \code{sigma} (numeric matrix or
\code{NULL}), and \code{ofv} (numeric scalar or \code{NA}).
}
\description{
Extracts the embedded control stream and the final parameter estimates
(THETA vector, OMEGA and SIGMA covariance matrices, and the objective
function value) from a NONMEM report/output file. This is useful when only
the report file is available and the \code{.ext} file (which pharmpy normally
reads final estimates from) is missing.
}
\details{
\strong{Precision:} the \verb{FINAL PARAMETER ESTIMATE} block in a \code{.lst} is rounded to
roughly three significant figures, so estimates recovered this way are lower
precision than those in the corresponding \code{.ext} file. Use the \code{.ext} file
(via \code{\link[=create_model_from_file]{create_model_from_file()}}) when full precision is required.

Standard errors and covariance-step output are not parsed. OMEGA/SIGMA
parsing handles the single column block NONMEM prints for up to six random
effects; wider matrices, which NONMEM splits across multiple column blocks,
are not yet supported.
}
