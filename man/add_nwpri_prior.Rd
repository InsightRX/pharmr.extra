% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_nwpri_prior.R
\name{add_nwpri_prior}
\alias{add_nwpri_prior}
\title{Add a \verb{$PRIOR NWPRI} record set to a NONMEM model}
\usage{
add_nwpri_prior(
  model,
  fit = NULL,
  plev = 0.9999,
  parameter_estimates = NULL,
  covariance_matrix = NULL
)
}
\arguments{
\item{model}{a Pharmpy NONMEM model object.}

\item{fit}{a Pharmpy modelfit object carrying \code{parameter_estimates} and
\code{covariance_matrix}. Ignored when \code{parameter_estimates} and
\code{covariance_matrix} are given directly.}

\item{plev}{probability mass the THETA draws are truncated to, emitted as
\verb{$PRIOR NWPRI PLEV=}. NONMEM has no default that works with
\code{TRUE=PRIOR} (it stops with \verb{VALUE OF ARGUMENT 'PLEV' IS INAPPROPRIATE}),
so this is always written out. The default is close enough to 1 that
truncation is negligible.}

\item{parameter_estimates}{named numeric vector of point estimates,
overriding \code{fit$parameter_estimates}.}

\item{covariance_matrix}{parameter uncertainty covariance matrix with
parameter names as row/column names, overriding \code{fit$covariance_matrix}.}
}
\value{
a Pharmpy NONMEM model object with the prior records added.
}
\description{
Builds the NONMEM normal-inverse-Wishart prior records (\verb{$PRIOR NWPRI},
\verb{$THETAP}, \verb{$THETAPV}, \verb{$OMEGAP}, \verb{$OMEGAPD}, \verb{$SIGMAP}, \verb{$SIGMAPD}) that
let NONMEM itself draw parameter vectors from a fit's uncertainty
distribution, and inserts them into the model code.
}
\details{
Combined with \verb{$SIMULATION ... TRUE=PRIOR} (see
\code{\link[=set_simulation_clean]{set_simulation_clean()}}) this is the \code{uncertainty_engine = "nwpri"} route
of \code{\link[=run_sim]{run_sim()}}: NONMEM draws a fresh parameter vector for every simulation
subproblem, so \code{n} uncertainty replicates cost one NONMEM compile rather
than \code{n}.
}
\section{How the prior is parameterised}{


The prior is centred on the fit, so simulating from it reproduces the fit's
parameter uncertainty rather than adding information to it.
\itemize{
\item \verb{$THETAP} holds the THETA point estimates and \verb{$THETAPV} the THETA block
of the covariance matrix, i.e. a multivariate normal prior on THETA.
\item \verb{$OMEGAP} / \verb{$SIGMAP} hold the OMEGA and SIGMA point estimates, mirroring
the block structure of the model's own \verb{$OMEGA} / \verb{$SIGMA} records.
\item \verb{$OMEGAPD} / \verb{$SIGMAPD} hold the inverse-Wishart degrees of freedom, one
per block, chosen so the prior variance of each block matches the
estimated standard error: \code{df = 2 * mean((estimate / se)^2) + p} over the
block's \code{p} diagonal elements (Gisleskog, Karlsson and Beal 2002; the
same rule PsN uses, which for the usual \code{p = 1} diagonal element reduces
to \code{2 * (estimate / se)^2 + 1}).
}

Standard errors are taken as the square root of the covariance matrix
diagonal rather than from \code{fit$standard_errors}, so the emitted records are
internally consistent with the covariance matrix they came from.

Because NWPRI treats the THETA prior, each OMEGA block and each SIGMA block
as independent, the THETA-OMEGA and THETA-SIGMA covariances that
\verb{$COVARIANCE} reports are \emph{not} carried into the prior. See the
\code{uncertainty_engine} section of \code{\link[=run_sim]{run_sim()}} for what that means in practice.
}

\section{Parameters not covered by the covariance matrix}{


Fixed parameters, and any other parameter the covariance matrix does not
cover, cannot be given an uncertainty. They still have to appear in the
prior records (NONMEM requires the prior to mirror the model's parameter
structure), so they are emitted with a negligible prior variance
(a relative standard deviation of \code{1e-3}) and are therefore held at their
point estimate for all practical purposes. A warning lists them.
}

\examples{
\dontrun{
fit <- run_nlme(model = model, data = data)
prior_model <- add_nwpri_prior(attr(fit, "final_model"), fit)
}

}
