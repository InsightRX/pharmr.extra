% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_sim_nlmixr.R
\name{run_sim_nlmixr}
\alias{run_sim_nlmixr}
\title{Run a simulation against an nlmixr-format model with rxode2}
\usage{
run_sim_nlmixr(
  fit = NULL,
  data = NULL,
  model = NULL,
  id = irxutils::get_random_id("sim_"),
  path = NULL,
  n_iterations = 1,
  variables = NULL,
  add_pk_variables = FALSE,
  output_file = "simtab",
  seed = 12345,
  verbose = TRUE,
  model_code = NULL
)
}
\arguments{
\item{fit}{a Pharmpy modelfit object.}

\item{data}{a NONMEM-format data.frame to use as the simulation dataset.
Typically the output of \code{\link[=create_sim_dataset]{create_sim_dataset()}}. If \code{NULL}, the dataset
attached to \code{model} is used as-is.}

\item{model}{either a Pharmpy model object, or a filename (for a model
with NONMEM model code). If the latter, \code{run_sim()} will attempt to load the
model into Pharmpy first.}

\item{id}{base run id (default a random \verb{sim_*}). Each regimen is run in its
own subfolder \verb{id/regimen_<i>} (\verb{<i>} = 1-based regimen index), so regimens
don't overwrite each other's output. Under
\code{uncertainty_engine = "replicates"} each draw gets a folder of its own too,
\verb{id/uncertainty_<r>/regimen_<i>} (\verb{<r>} = 1-based replicate index), so every
replicate's NONMEM artifacts can be inspected afterwards and concurrent
replicates cannot clobber each other.}

\item{path}{ignored for the nlmixr2 backend: simulations run via
\code{\link[rxode2:rxSolve]{rxode2::rxSolve()}} and create no NONMEM-style run folders. Accepted only to
keep the signature aligned with \code{\link[=run_sim]{run_sim()}}.}

\item{n_iterations}{number of iterations of the entire simulation to
perform. The dataset for the simulation will stay the same between each
iterations.}

\item{variables}{vector of variables to output. If \code{NULL}, will output
default variables \code{c("ID", "TIME", "DV", "EVID", "PRED")} as well as
all variables declared in the NONMEM code.}

\item{add_pk_variables}{calculate basic PK variables: CMAX_OBS, TMAX_OBS,
CMIN_OBS, and (when \code{CL} is in the output table) AUC_SS. AUC_SS is derived
as the last dose in the simulation dataset divided by CL.}

\item{output_file}{TODO}

\item{seed}{TODO}

\item{verbose}{verbose output?}

\item{model_code}{pre-rendered nlmixr2/rxode2 model code (character), used
instead of extracting it from \code{model}. This is what lets a simulation run in
a worker process: a Pharmpy \code{model} is a Python (reticulate) object and
cannot cross a process boundary, but its rendered code can. When supplied,
\code{model}/\code{fit} are not needed and \code{data} is required.}
}
\description{
Internal companion to \code{\link[=run_sim]{run_sim()}}; called when the input model is a
pharmpy nlmixr-backend model. Uses \code{\link[rxode2:rxSolve]{rxode2::rxSolve()}} directly so we
can avoid the pharmpy-driven nlmixr fitting/simulation path (which
requires the Python \code{pyreadr} package).
}
\details{
Returns a data.frame in the same shape as the NONMEM-side simulation
output (\code{ID}, \code{TIME}, \code{DV}, \code{IPRED}, \code{PRED}, \code{EVID}, plus declared
variables and a \code{regimen_label} column), so downstream example code
that plots simulation results works unchanged.

\code{PRED} is the population prediction, obtained from a second solve with the
between-subject random effects zeroed (\code{\link[rxode2:zeroRe]{rxode2::zeroRe()}}) — rxSolve cannot
emit \code{IPRED} and \code{PRED} in one call. If that solve is unavailable (older
rxode2 without \code{zeroRe()}) or fails, \code{PRED} falls back to \code{IPRED}.
}
\keyword{internal}
