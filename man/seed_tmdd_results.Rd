% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_pharmpy_tool.R
\name{seed_tmdd_results}
\alias{seed_tmdd_results}
\title{Seed TMDD target-binding parameters into a base-fit results object}
\usage{
seed_tmdd_results(results, kd, verbose = TRUE)
}
\arguments{
\item{results}{a Pharmpy \code{ModelfitResults} object (e.g. from \code{\link[=run_nlme]{run_nlme()}}).}

\item{kd}{target dissociation constant seed (concentration units).}

\item{verbose}{emit an info message?}
}
\value{
a \code{ModelfitResults} with augmented \code{parameter_estimates}.
}
\description{
Returns a copy of \code{results} whose \code{parameter_estimates} gains \code{POP_KM} (= \code{kd})
and \code{POP_CLMM} (= the fitted \code{POP_CL}). Pharmpy's TMDD \code{structsearch} uses these
to seed the QSS candidates' \code{POP_KDC} / \code{POP_KINT}; without them the stiff TMDD
ODE diverges. Used internally by \code{\link[=call_pharmpy_tool]{call_pharmpy_tool()}} when \code{tool = "structsearch"},
\code{options$type = "tmdd"} and \code{options$kd} is supplied. If the results already carry
\code{POP_KM}/\code{POP_CLMM} (an MM-parameterised base) the object is returned unchanged.
}
\keyword{internal}
