# Parse \$INPUT record lines into a data frame

Parse \$INPUT record lines into a data frame

## Usage

``` r
.parse_nm_input(input_lines)
```

## Arguments

- input_lines:

  Character vector of lines from the `$INPUT` record.

## Value

A data frame with columns `nonmem_name` and `dropped`.
