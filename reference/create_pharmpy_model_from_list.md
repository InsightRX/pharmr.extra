# Create a model object from the model code and dataset stored as a list object.

Create a model object from the model code and dataset stored as a list
object.

## Usage

``` r
create_pharmpy_model_from_list(model_obj)
```

## Arguments

- model_obj:

  list object with entries `code` and `dataset`

## Value

(Model) Pharmpy model object
