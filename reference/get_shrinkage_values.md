# Get shrinkage values from a single line in NONMEM output

Get shrinkage values from a single line in NONMEM output

## Usage

``` r
get_shrinkage_values(txt, type = "ETASHRINKSD")
```

## Arguments

- txt:

  TODO

- type:

  TODO
