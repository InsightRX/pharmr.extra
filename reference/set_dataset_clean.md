# Drop-in replacement for pharmr::set_dataset_clean that avoids any type-conversion issues.

Drop-in replacement for pharmr::set_dataset_clean that avoids any
type-conversion issues.

## Usage

``` r
set_dataset_clean(model, path_or_df)
```

## Arguments

- path_or_df:

  path to data file or data.frame / tibble

## Value

Pharmpy model object
