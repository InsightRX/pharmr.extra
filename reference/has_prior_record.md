# Does a NONMEM model (object or code) carry a `$PRIOR` record?

Does a NONMEM model (object or code) carry a `$PRIOR` record?

## Usage

``` r
has_prior_record(model)
```

## Arguments

- model:

  a Pharmpy NONMEM model object, or NONMEM model code.

## Value

`TRUE` or `FALSE`.
