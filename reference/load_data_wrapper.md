# Wrapper to load data from either filename or data.frame

Wrapper to load data from either filename or data.frame

## Usage

``` r
load_data_wrapper(data)
```

## Arguments

- data:

  filename of dataset or data.frame as input to NONMEM / nlmixr.

## Value

data.frame or NULL
