# Print nmfe output (stdout and stderr) from a run folder

Print nmfe output (stdout and stderr) from a run folder

## Usage

``` r
print_nmfe_output(nmfe_output)
```

## Arguments

- nmfe_output:

  output from nmfe command, as list
