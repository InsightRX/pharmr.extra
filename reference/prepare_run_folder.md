# Create a folder for running model, with the model and dataset

Create a folder for running model, with the model and dataset

## Usage

``` r
prepare_run_folder(
  id,
  model,
  path,
  force = FALSE,
  data = NULL,
  auto_stack_encounters = FALSE,
  verbose = TRUE
)
```
