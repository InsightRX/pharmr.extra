# Parses a NONMEM output file and extracts shrinkage

Parses a NONMEM output file and extracts shrinkage

## Usage

``` r
get_shrinkage_summary(path = NULL, fit = NULL)
```

## Arguments

- path:

  path to nonmem output file (.lst)

- fit:

  pharmpy model object
