# Get covariate data (single row per subject) from a dataset given model

Get covariate data (single row per subject) from a dataset given model

## Usage

``` r
get_covariate_data(model)
```

## Arguments

- model:

  Pharmpy NONMEM model object

## Value

a data.frame or tibble with covariates for model
