# extract FILE names from \$TABLE using simple regex. For some reason the tables are not (yet?) available in pharmpy

extract FILE names from \$TABLE using simple regex. For some reason the
tables are not (yet?) available in pharmpy

## Usage

``` r
get_tables_in_model_code(code)
```

## Arguments

- code:

  a character string with NONMEM model code

## Value

TODO
