#' Run a PsN tool
#'
#' @param model TODO
run_psn <- function(
  model
) { # nocov start
  # TODO: write function code or deprecate. Remove nocov after writing code.
  
  # cmd <- paste0(cmd, "execute ", model_file, " --dir=", fit_dir)
} # nocov end
